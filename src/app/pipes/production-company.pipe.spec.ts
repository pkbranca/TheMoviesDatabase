import { ProductionCompanyPipe } from './production-company.pipe';

describe('ProductionCompanyPipe', () => {
  it('create an instance', () => {
    const pipe = new ProductionCompanyPipe();
    expect(pipe).toBeTruthy();
  });
});
