import { MovieBackgroundPipe } from './movie-background.pipe';

describe('MovieBackgroundPipe', () => {
  it('create an instance', () => {
    const pipe = new MovieBackgroundPipe();
    expect(pipe).toBeTruthy();
  });
});
